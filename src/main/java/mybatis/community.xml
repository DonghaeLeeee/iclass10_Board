<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="community">
	<!-- Community.java vo 클래스를 정의하고 하세요. 
	1. 메인글 글쓰기 : insert 
	2. 글내용수정 : update 
	3. 글삭제 : delete 
	4. 글 보기 : select -->
	<insert id="insert" parameterType="org.iclass.vo.Community">
		INSERT INTO COMMUNITY
		VALUES
		(#{idx},#{writer},#{title},#{content},#{readCount},sysdate,#{ip},#{commentCount})
	</insert>

	<update id="update" parameterType="org.iclass.vo.Community">
		UPDATE COMMUNITY
		SET content = #{content}
		WHERE id=#{id}
	</update>


	<delete id="delete">
		DELETE FROM COMMUNITY
		WHERE id=#{id}
	</delete>


	<select id="selectOne" parameterType="String"
		resultType="org.iclass.vo.NewMember">
		SELECT * FROM COMMUNITY WHERE id=#{id}
	</select>

</mapper>